<form method="post" action="{{ path('app_video_game_delete', {'id': video_game.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ video_game.id) }}">
    <button class="btn">Delete</button>
</form>
